using System;
using System.IO.Compression;

public class Archivator
{

    public Archivator()
    {
        //ZipArchive archive = ZipFile.OpenRead(pathSave);

        //foreach (ZipArchiveEntry entry in archive.Entries)
        //{
           // string path = System.IO.Path.Combine(extractpath, entry.FullName);
           // entry.ExtractToFile(path);
       // }
       // archive.Dispose();
    }

  

}



           